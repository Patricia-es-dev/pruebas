export default function (props) {
  //TODO:  formatear decentemente
    return {
      username: props.username,
      name: props.name,
      honor: props.honor,
      clan: props.clan,
      leaderboardPosition: props.leaderboardPosition,
      languages: props.languages,
      codeChallenges: props.codeChallenges,
      skills: props.skills,
      ranks: props.ranks
    }
  }